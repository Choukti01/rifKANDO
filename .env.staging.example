# Browser-visible values only. Configure these in a separate staging site;
# never reuse live credentials or put backend secrets in VITE_* variables.
VITE_API_URL=https://your-staging-backend.example.com/api
VITE_GOOGLE_CLIENT_ID=your-staging-google-web-client-id.apps.googleusercontent.com
