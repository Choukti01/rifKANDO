# Create /opt/rifkando/.cloudflared.env with chmod 600 after creating the
# replacement tunnel in Cloudflare. This token is a credential: never commit it.
CLOUDFLARE_TUNNEL_TOKEN=replace-with-new-scoped-tunnel-token

